---
export async function getStaticPaths() {
  const pages = ["/"];

  return pages.map((page) => {
    return {
      params: { slug: page == "/" ? undefined : page },
    };
  });
}
---

<div class="container max-w-3xl mx-auto p-4">
  <img src="/logo.png" alt="logo" class="w-full max-w-sm mx-auto mt-12 mb-12" />
  <div class="flex flex-col items-center gap-24">
    <h2 class="text-5xl font-bold underline"><a href="">Spotify</a></h2>
    <h2 class="text-5xl font-bold underline"><a href="">Apple music</a></h2>
    <h2 class="text-5xl font-bold underline"><a href="">YouTube</a></h2>
    <p class="underline">
      <a href="https://tamangox3.it">tamangox3.it</a>
    </p>
  </div>
</div>
